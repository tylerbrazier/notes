For encryption:
	public key encrypts, private decrypts
	(anyone can send you a message)
Reverse for authentication: 
	private signs, public verifies
	(anyone can see your signature)

--- Oauth ---
OAuth 2.0 is a framework (not rigid protocol) for authZ.
- doesn't define token format - could be string or JWT
- doesn't define what scopes are possible - that's up to the id provider

OpenID is standard protocol for authN.
- v1 and v2 had problems
- today "OpenID" almost always means "OpenID Connect"

OpenID Connect (OIDC) built on top of OAuth 2.0.
- defines mandatory "openid" scope, and other optional standard scopes
- uses signed JWT as the ID token

SSO is a concept (implemented by SAML or OIDC)

https://developer.okta.com/docs/concepts/oauth-openid/
https://infosec.mozilla.org/guidelines/iam/openid_connect.html

OAuth provides the secure communication channels and the mechanism for requesting
tokens, while OIDC defines a standard language to speak over those channels.

--- Oauth Scopes ---
Scopes are an Oauth 2.0 concept.
They're sent by the app to request permissions from IdP/OP.
OIDC required scope is "openid".
OIDC optional standard scopes:
https://openid.net/specs/openid-connect-core-1_0.html#ScopeClaims
- profile
- email
- address
- phone
IdP/OP then responds with claims for those scopes.

--- Oauth Claims ---
IdP/OP responds with claims that are associated with requested scopes.
There can be multiple claims per requested scope.
Think of scopes as buckets for claims.
OIDC standard claims:
https://openid.net/specs/openid-connect-core-1_0.html#StandardClaims

--- OIDC Flow ---

"Client" App       IdP
  (service)      (aka OP)
        ^   \     ^  /
         \   2   /  3
          1   \ /  /
           \   v  v
            Browser

1. user clicks "login with Google/Github/etc."
2. App redirects browser to IdP /authorize with
   - client_id
   - scope (app wants access to? openid email profile)
   - redirect_uri
3. IdP shows login page
   - after login, IdP says "allow App to access email & profile?"

"Client" App --5--> IdP
 (service)  <--6-- (aka OP)
           ^     /   
            \   4   
             \ /   
              v   
           Browser

4. IdP sends browser to redirect_uri with
   - code (short lived)
5. App calls /token with
   - code
   - client_id
   - client_secret (otherwise anyone could call /token)
6. IdP responds with JSON
   - id_token (signed JWT w/ claims iss,sub,aud,exp,email,name,picture,etc)
   - access_token (used to call other apis on behalf of user; often JWT)
At this point the Client checks the signature of the id_token
